# 学习记录（按天 · Day 1-10）

> 每天学了哪些新东西，以及它在当天的代码里长什么样。
> 复习方法见文末。

---

# 第 1 天 · 2026-09-09 · `index.html`、`day01.html`

**目标**：让代码跑起来，理解"网页 + 脚本"是什么。

## 新东西

### ① HTML 骨架（固定模板）

```html
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">        <!-- 声明编码，不加中文会乱码 -->
  <title>标签页上的字</title>
</head>
<body>
  <!-- 用户能看到的内容都写这里 -->
</body>
</html>
```

- 骨架基本固定，每次只改 `<title>` 和 `<body>` 里的东西
- VS Code 里输入 `!` 再按 Tab 能一键生成
- `<script>` 标签用来装 JavaScript，**要放在它用到的元素后面**

### ② 变量 `let`

```javascript
let name = prompt("你叫什么名字？");
```

**变量 = 一个盒子**。`let` 是"我要一个新盒子"，`name` 是盒子上的标签，`=` 是把东西放进去。

**命名可以是中文**：`let 姓名 = ...` 也完全合法（你后面几天一直在用）。

### ③ `prompt()` —— 弹出输入框

```javascript
let name = prompt("你叫什么名字？");      // 弹出输入框，把用户输入的东西返回出来
```

**重要**：`prompt` 拿到的**永远是字符串**，哪怕用户输入的是数字。

### ④ `alert()` —— 弹出提示框

```javascript
alert("你好，" + name + "！");
```

### ⑤ 字符串拼接 `+`

```javascript
"你好，" + name + "！"        // 把变量和文字拼成一句话
```

**`+` 有两个含义**：数字相加 / 字符串拼接。只要有一边是字符串，就是拼接。

### ⑥ `console.log()` —— 输出到控制台

```javascript
console.log("程序跑通了");
```

按 **F12** → **Console** 才能看到。

## 三种输出方式的区别

| 方式 | 输出到哪 |
|---|---|
| `console.log()` | F12 控制台（只有自己看得到） |
| `alert()` | 弹窗 |
| 页面元素 | 页面（第 5 天才学） |

---

# 第 2 天 · 2026-09-10 · `day02.html`

**目标**：让程序能"做选择"。

## 新东西

### ① `if / else if / else` 条件判断

```javascript
let age = Number(prompt("您的年龄是多少？"));

if (age < 6) {
    alert("您 " + age + " 岁，票价：免费");
} else if (age <= 17) {
    alert("您 " + age + " 岁，票价：半价 50 元");
} else if (age <= 59) {
    alert("您 " + age + " 岁，票价：全价 100 元");
} else {
    alert("您 " + age + " 岁，票价：老年票 30 元");
}
```

**关键理解**：`else if` 是**按顺序判断**的。走到第二个条件时，说明第一个已经不成立了 —— 所以**不用重复写上界**（`age <= 17` 就够了，不用写 `age >= 6 && age <= 17`）。

### ② 比较运算符

```javascript
>    <    >=    <=
===      // 等于（三个等号）
!==      // 不等于
```

**⚠️ 判断相等必须用 `===`**，一个等号 `=` 是赋值。

### ③ `Number()` 类型转换

```javascript
let age = Number(prompt("您的年龄是多少？"));
```

**为什么必须转**：`prompt` 返回的是**字符串** `"20"`，不是数字 `20`。

**这次踩的坑**：写成

```javascript
let age = prompt("...");
Number(age);              // ❌ 这行什么也没干
```

`Number(age)` 只是"算出"一个数字然后扔掉，**不会改变 `age`**。必须写成 `age = Number(age)` 或者合并成一句。

**规律**：

```javascript
let age = prompt("...");            // age 是字符串 "20"
let age = Number(prompt("..."));    // age 是数字 20
```

---

# 第 3 天 · 2026-09-10 · `day03.html`

**目标**：让代码"重复执行"。

## 新东西

### ① `for` 循环

```javascript
for (let i = 1; i <= 9; i++) {
    // 重复执行的代码
}
```

拆成三部分：

| 部分 | 含义 |
|---|---|
| `let i = 1` | 开始时：创建计数器 `i`，从 1 开始 |
| `i <= 9` | 每轮开始前检查：不满足就停 |
| `i++` | 每轮结束后：`i` 加 1 |

### ② 双层循环

```javascript
for (let i = 1; i <= 9; i++) {        // 外层：第几行
    for (let j = 1; j <= i; j++) {    // 内层：这一行有几个
        console.log(j + "×" + i + "=" + j * i);
    }
}
```

**关键**：内层循环的终止条件是 `j <= i`（跟着外层变），**不是固定值**。

### ③ 累积模式（重要）

```javascript
let row = "";                          // ① 循环前准备一个空容器
for (let j = 1; j <= i; j++) {
    row += j + "×" + i + "=" + j*i + "  ";    // ② 循环里往里加
}
console.log(row);                      // ③ 循环外一次性输出
```

**"循环里攒，循环外交"** —— 这个模式以后会反复用到。

**⚠️ 这次踩的坑**：把 `console.log(row)` 写在了内层循环里，导致 45 个算式竖着排成 45 行，而不是每行一行。

---

# 第 4 天 · 2026-09-10 · `day04.html`

**目标**：一次存很多数据。

## 新东西

### ① 数组

```javascript
let 分数 = [];                 // 空数组
分数.push(85);                 // 往末尾加一个
分数[0]                        // 取第 1 个（下标从 0 开始）
分数.length                    // 有几个元素
```

| 写法 | 作用 |
|---|---|
| `数组[0]` | 取第一个（**下标从 0 开始，不是 1**） |
| `数组.length` | 元素个数 |
| `数组.push(值)` | 末尾追加（**会改原数组**） |

### ② 遍历数组的标准写法

```javascript
for (let i = 0; i < 分数.length; i++) {
    console.log(分数[i]);
}
```

**注意是 `i < length`，不是 `<=`** —— 最后一个元素的下标是 `length - 1`。

### ③ `join()` —— 数组转字符串

```javascript
let 成绩 = 分数.join(",");     // [85,92,78] → "85,92,78"
```

**没学之前**，这功能是手写 if/else 实现的（"最后一个不加逗号"），很啰嗦。

### ④ 找最大 / 最小值（打擂台算法）

```javascript
let 最高分 = 分数[0];                  // ① 先假设第一个是最高
for (let i = 0; i < 分数.length; i++) {
    if (分数[i] > 最高分) {            // ② 逐个挑战
        最高分 = 分数[i];              //    赢了就换擂主
    }
}
```

**初始值的关键**：必须是**数组里的真实数据**（或者"足够小/足够大"的值）。

- ✅ `分数[0]` —— 真实数据，一定不会越界
- ✅ `-Infinity` —— 绝对最小，任何数据都能更新它
- ❌ `0` —— 如果所有分数都是负数，会错误地返回 0
- ❌ `100` —— 如果有超过 100 的分数（比如总分），就卡住了

**规律**：求最大值 → 初始值要"足够小"；求最小值 → 初始值要"足够大"。

---

# 第 5 天 · 2026-09-11 · `day05.html`

**目标**：让内容显示在网页上（而不是控制台里）。

## 新东西

### ① `document.getElementById()` —— 找到页面元素

```html
<div id="表格"></div>
```

```javascript
let 容器 = document.getElementById("表格");
```

**关键**：HTML 里要给元素加 `id="xxx"`，JS 才能通过 `getElementById("xxx")` 找到它。**两处名字必须一模一样。**

**"容器"就是页面上的一块空白区域** —— 先有个空位，JS 再往里填内容。

### ② `innerHTML` —— 往里填内容

```javascript
容器.innerHTML = 全部内容;
```

**⚠️ 元素必须先存在**：`<script>` 要放在 `<div id="表格">` **后面**，否则报 `Cannot read properties of null`。

### ③ `innerText` vs `innerHTML`

| 属性 | `<br>` 会怎样 |
|---|---|
| `innerText` | 被当成**普通文字**显示出来 |
| `innerHTML` | **真正生效**（换行） |

**要生成元素（`<span>`、`<div>`）必须用 `innerHTML`。**

### ④ 动态生成内容

```javascript
let 全部内容 = "";
for (let i = 1; i <= 9; i++) {
    for (let j = 1; j <= i; j++) {
        全部内容 += '<span class="item">' + j + "×" + i + "=" + j*i + '</span>';
    }
    全部内容 += "<br>";           // ← 每行结束换行（写在内层循环外、外层循环内）
}
容器.innerHTML = 全部内容;         // ← 循环外一次性填入
```

**为什么不能手写 HTML**：45 个算式手敲不现实，而且以后数据会变。**JS 的价值就是"内容能根据数据自动变化"。**

---

# 第 6 天 · 2026-09-12 · `day06.html`

**目标**：让页面好看。

## 新东西

### ① CSS 写在哪

```html
<head>
    <style>
        /* CSS 写这里 */
    </style>
</head>
```

### ② 三种选择器

```css
#表格 { }         /* # = 按 id 选（唯一元素）*/
h1 { }           /* 直接写标签名 = 所有该标签 */
.item { }        /* . = 按 class 选（可重复用）*/
.item:hover { }  /* 伪类：鼠标悬停时 */
```

### ③ 常用属性

| 属性 | 作用 |
|---|---|
| `color` | 文字颜色 |
| `background-color` | 背景色 |
| `padding` | **内**边距（内容到边框的距离） |
| `margin` | **外**边距（元素之间的距离） |
| `border-radius` | 圆角 |
| `width` | 宽度 |
| `text-align` | 文字对齐 |

**`margin` vs `padding` 记法**：`margin` 推别人（外面），`padding` 撑自己（里面）。

### ④ `display: inline-block`

```css
.item {
    display: inline-block;      /* 能排成一行 + 能设宽高 */
}
```

| 值 | 效果 |
|---|---|
| `inline`（`<span>` 默认） | 挤在一行，**不能设宽高** |
| `block`（`<div>` 默认） | 独占一行 |
| **`inline-block`** | **两者之间：能排一行又能设尺寸** |

### ⑤ 盒模型与 `box-sizing`

```
默认（content-box）：
总宽度 = width + padding(左右) + border(左右) + margin(左右)

加了 box-sizing: border-box 之后：
总宽度 = width（含 padding 和 border） + margin(左右)
```

```css
.item {
    box-sizing: border-box;
    width: 90px;            /* 总宽就是 96px（90 + margin 3×2），好算 */
    padding: 5px;
    margin: 3px;
}
```

### ⑥ 布局计算（重要）

```
一行能放几个 = 容器宽度 ÷ 单个元素总宽度（向下取整）
```

```css
#表格 { max-width: 900px; }
.item { width: 90px; margin: 3px; }    /* 每个占 96px */
/* 900 ÷ 96 = 9.375 → 一行 9 个 */
```

### ⑦ 居中

```css
#表格 {
    max-width: 900px;
    margin: 0 auto;         /* 左右自动 = 水平居中 */
    text-align: center;     /* 里面的行内元素居中 */
}
```

---

# 第 7 天 · 2026-09-13 · `day07.html`

**目标**：让页面能对用户操作做出反应。

## 新东西

### ① `addEventListener` —— 事件监听

```javascript
元素.addEventListener("click", function () {
    // 点击时才执行
});
```

**读法**："盯着这个元素，一旦有人点击它，就执行这段代码。"

| 事件名 | 触发时机 |
|---|---|
| `"click"` | 点击 |
| `"input"` | 输入框内容变化（边打边触发） |
| `"change"` | 输入完成 / 状态变化 |

### ② 回调函数（核心概念）

```javascript
按钮.addEventListener("click", function () {
    console.log("被点了");        // ← 现在不执行，等点击时才执行
});
```

**关键**：`function () { ... }` 里的代码**现在不会跑**，只是"登记"进去。**这和你之前学的顺序执行完全不同。**

**⚠️ 传函数时不要加括号**：

```javascript
addEventListener("click", 处理);       // ✅ 传函数本身
addEventListener("click", 处理());     // ❌ 加了括号 = 立刻执行
```

### ③ 读输入框的值

```html
<input id="范围" type="number">
```

```javascript
let n = Number(输入框.value);      // .value 拿到的是字符串！
```

### ④ `return` —— 提前结束函数

```javascript
if (n < 1) {
    alert("请输入 1 以上的数字");
    return;                 // ← 函数立刻结束，后面的代码不执行
}
```

### ⑤ 作用域（这次踩了大坑）

```javascript
let 全部内容 = "";              // ❌ 写在事件外面

按钮.addEventListener("click", function () {
    全部内容 += "...";          // 每次点击都在旧内容上继续累加
    容器.innerHTML = 全部内容;   // → 页面越滚越长
});
```

**正解**：把变量挪到**事件里面**：

```javascript
按钮.addEventListener("click", function () {
    let 全部内容 = "";          // ✅ 每次点击都是新的空字符串
    // ...
});
```

**规律**：

| 变量写在哪 | 行为 | 什么时候用 |
|---|---|---|
| 事件**外面** | 一直活着，**记住上次的值** | 计数器、状态 |
| 事件**里面** | **每次都是全新的** | 每次要重新拼的内容 |

**判断口诀**：**"这个变量需要记住上一次吗？"**

---

# 第 8 天 · 2026-09-16 · `day08.html`

**目标**：让数据在刷新后还在。

## 新东西

### ① `localStorage`

```javascript
localStorage.setItem("键名", "值")       // 存
localStorage.getItem("键名")             // 取（不存在时返回 null）
localStorage.removeItem("键名")          // 删
```

**它只能存字符串。**

### ② `JSON.stringify` / `JSON.parse`

```javascript
JSON.stringify(数组)       // 打包：数组 → 字符串
JSON.parse(字符串)         // 拆包：字符串 → 数组
```

**为什么需要**：数组/对象不能直接存。

```javascript
localStorage.setItem("水果", ["苹果","香蕉"]);     // ❌ 被强制转成 "苹果,香蕉"
                                                  //    对象会变成 "[object Object]"，结构全丢
localStorage.setItem("水果", JSON.stringify(["苹果","香蕉"]));   // ✅ 存 '["苹果","香蕉"]'
```

**读回来必须拆包**，否则拿到的是字符串，不能用 `.push`。

### ③ `||` 兜底

```javascript
let 待办列表 = JSON.parse(localStorage.getItem("待办")) || [];
```

**为什么需要**：第一次打开页面时，`getItem` 返回 `null`，`JSON.parse(null)` 得到 `null`，再对 `null` 用 `.push` 就会报错。

### ④ 渲染模式（核心）

```javascript
function 渲染() {
    let 内容 = "";
    for (let i = 0; i < 待办列表.length; i++) {
        内容 += '<div class="待办项">' + 待办列表[i] + '</div>';
    }
    列表容器.innerHTML = 内容;          // ← 循环外面！
}
```

**⚠️ 只调用一次 `渲染()` 不够** —— 数据改变后要**再调一次**，页面才会更新。

### ⑤ 完整数据流

```
打开页面 → 读数据 → 渲染()
用户操作 → 改数组 → 存数据 → 渲染()
```

### 这次踩的四个坑

| # | 坑 | 正解 |
|---|---|---|
| 1 | `[] \|\| 读数据` 顺序反了 | `[]` 是真值，永远返回它 → 写成 `读数据 \|\| []` |
| 2 | `innerHTML =` 写在循环里 | 写循环**外面**（数组为空时循环不执行，旧内容不会被清掉） |
| 3 | 忘了页面打开时先 `渲染()` | 脚本最后加一行 `渲染();` |
| 4 | 删按钮的 `onclick` 多打了一个引号 | 仔细检查生成的 HTML 字符串 |

**顺带学到的**：**只有 6 个假值** —— `false`、`0`、`""`、`null`、`undefined`、`NaN`。**其他全是真值，包括 `[]` 和 `{}`。**

---

# 第 9 天 · 2026-09-17 · `day09.html`

**目标**：一条待办记录更多信息，代码更整洁。

## 新东西

### ① 对象 `{ }`

```javascript
let 一条待办 = {
    文字: "写作业",
    完成: false
};

一条待办.文字          // "写作业"     ← 用点号取值
一条待办.完成 = true;   // 改属性
```

**对象 vs 数组**：

| | 数组 `[ ]` | 对象 `{ }` |
|---|---|---|
| 存什么 | 一串**同类**的东西 | 一组**相关的不同信息** |
| 怎么取 | 用**下标** `列表[0]` | 用**名字** `待办.文字` |
| 适合 | 一批待办 | **一条**待办 |

### ② 数组装对象（最重要的结构）

```javascript
let 待办列表 = [
    { 文字: "写作业", 完成: false },
    { 文字: "学 JS",  完成: true  }
];

待办列表[0].文字        // "写作业"
待办列表.push({ 文字: "买菜", 完成: false });
```

**这个结构以后会一直用到** —— 从后端拿数据、课程项目的记录列表，全是这个形状。

### ③ 模板字符串（反引号）

```javascript
内容 += `
<div class="待办项">
    <label class="待办内容">
        <input type="checkbox" onclick="勾选(${i})" ${这条.完成 ? "checked" : ""}>
        <span class="${这条.完成 ? "已完成" : ""}">${这条.文字}</span>
    </label>
    <button class="删除按钮" onclick="删除(${i})">删除</button>
</div>`;
```

**对比旧的 `+` 拼接**：

```javascript
内容 += '<div class="待办项">'
    + '<input type="checkbox" onclick="勾选('
    + i
    + ')"'
    + (待办列表[i].完成 ? "checked" : "")
    + '>'
    // ...14 行
```

**好处**：HTML 原样保留，**不用再跟引号打架**。

| 关键点 | 说明 |
|---|---|
| 用**反引号** `` ` `` 包 | 键盘左上角，数字 1 左边 |
| 变量放进 **`${ }`** | `${i}`、`${这条.文字}` |
| **可以直接换行** | 不用写 `+` |
| `${ }` 里可写表达式 | `${这条.完成 ? "checked" : ""}` |

### ④ 三元运算符

```javascript
条件 ? 成立时的值 : 不成立时的值

这条.完成 ? "checked" : ""          // 完成就输出 checked，否则输出空
```

**if/else 的一行写法。**

### ⑤ `!` 取反

```javascript
待办列表[下标].完成 = !待办列表[下标].完成;      // 翻转
```

### ⑥ `<label>`

```html
<label class="待办内容">
    <input type="checkbox">
    写作业
</label>
```

**作用**：**点文字 = 点控件**。不加 `<label>` 时，用户必须精确点到那个小方框才能勾选。

```css
label { cursor: pointer; }      /* 鼠标变手型，提示"这里能点" */
```

### ⑦ 一个容器只装一类东西

**这次发现的布局问题**：统计文字和列表塞进了同一个容器（`列表容器.innerHTML = 统计 + 内容`），结果统计被当成了"第 4 条待办"，样式也串了。

**正解**：给统计一个自己的位置。

```html
<div id="统计"></div>
<div id="列表"></div>
```

```javascript
document.getElementById("统计").innerText = "已完成 " + 已完成数 + " / 共 " + 待办列表.length;
列表容器.innerHTML = 内容;      // 列表只管列表
```

### ⑧ 选择器要精确

```css
input { flex: 1; }        /* ❌ 影响页面上所有 input，包括勾选框 */
#输入框 { flex: 1; }      /* ✅ 只影响那一个 */
```

---

# 第 10 天 · 2026-09-19 · `day10.html`

**目标**：用数组方法替换手写的 `for` 循环。

## 新东西

### ① `forEach` —— 只是遍历

```javascript
let 已完成数 = 0;
待办列表.forEach(function (这条) {
    if (这条.完成) 已完成数++;
});
```

**替换掉的是 `for` 的"管下标"部分**：

| `for` 写法 | `forEach` 写法 |
|---|---|
| `let i = 0; i < 数组.length; i++` | **删掉**（自己会遍历） |
| `数组[i]` | 函数参数 `这条` |
| 循环体 | **原样保留** |

**`forEach` 不省 `if`** —— 它只省"管下标"，返回值会被丢掉。

### ② `map` —— 一个变一个

```javascript
let 内容 = 待办列表.map(function (这条, i) {
    return `
        <div class="待办项">
            <label class="待办内容">
                <input type="checkbox" onclick="勾选(${i})" ${这条.完成 ? "checked" : ""}>
                <span class="${这条.完成 ? "已完成" : ""}">${这条.文字}</span>
            </label>
            <button class="删除按钮" onclick="删除(${i})">删除</button>
        </div>`;
}).join("");            // ← 数组转字符串
```

**替换掉了两件事**：`for` 循环 + 字符串累加（`内容 += ...`）。

**返回新数组**（长度和原数组一样），里面是**每次 `return` 的值**。

**第二个参数是下标**：`function (元素, 下标)`。

### ③ `filter` —— 挑出符合条件的

```javascript
function 删除(下标) {
    待办列表 = 待办列表.filter(function (这条, i) {
        return i !== 下标;            // 保留"下标不等于要删的那个"
    });
    保存();
    渲染();
}
```

**替换掉了 `splice`**，读起来更像人话："保留除了这条以外的"。

**⚠️ 关键区别**：

| 方法 | 对原数组 |
|---|---|
| `splice(下标, 1)` | **直接改**原数组 |
| **`filter`** | **不改**，返回新数组 → **必须赋值回去** |

```javascript
待办列表 = 待办列表.filter(...);      // ← 这个等号不能少
```

**忘了赋值回去 = 删除没效果**，这是 `filter` 最常见的坑。

### ④ 三个方法的区别（重点）

| 方法 | 返回什么 | 用来干什么 | 能省掉 `if` 吗 |
|---|---|---|---|
| **`map`** | **新数组**（长度不变） | 转换每个元素 | ✅ 能 |
| **`filter`** | **新数组**（长度可能变） | 筛选 | ✅ 能 |
| **`forEach`** | **`undefined`** | 只是遍历做事 | ❌ 省不掉 |

**判断口诀**：

- **想在函数里 `return` 出一个新东西 → 用 `map`**
- **想筛掉一部分 → 用 `filter`**
- **只是想做点事（打印、累加、改页面）→ 用 `forEach`**

### ⑤ `.join("")` 配合 `map`

```javascript
let 内容 = 待办列表.map(这条 => `...`).join("");
                ↑ 返回数组        ↑ 数组转成字符串
```

`map` 得到的是**字符串数组**，要用 `join("")` 拼成一个大字符串才能填进 `innerHTML`。

---

# 关于这三个方法的一个诚实结论

**自己写代码时，用 `for` 完全没问题。**

| | `for` | `map` / `filter` / `forEach` |
|---|---|---|
| 学习成本 | 学一次到处用 | 要记三个 |
| 一次性做多件事 | ✅ 一个循环搞定 | 要拆成几段 |
| 代码长短 | 啰嗦 | 短 |
| **别人代码里的出现频率** | 少 | **多** |

**所以**：

- ✅ **自己写用 `for`** —— 别为了"看起来专业"硬用新方法
- ✅ **但要能看懂这三个** —— 因为读别人代码、看教程、用框架时到处都是
- ✅ **"筛选一个数组"这种典型场景，试 `filter`** —— 它确实更短

**`map`/`filter` 的价值要到代码规模变大、或者读别人代码时才真正体现。**

---

# 复习方法

**光"翻阅"没用** —— 读参考建立的是"认得"，自己写建立的是"想得起"，这是两种不同的记忆。

**每复习一节，走三步**：

1. **看** —— 翻这一节，2 分钟
2. **关掉，自己默写** —— 凭印象把那段代码写出来，3 分钟
3. **对照，标出差异** —— 写错/写漏的地方就是薄弱点，1 分钟

**重点在第 2 步。**

**建议复习顺序**（按重要性）：

| 优先 | 内容 | 理由 |
|---|---|---|
| 1 | 第 8 天的**完整数据流** | 最实用，也是花时间最多的 |
| 2 | 第 7 天的**作用域** | 踩过坑，以后每次写都会遇到 |
| 3 | 第 4、9、10 天的**数组与对象** | 数据操作的地基 |
| 4 | 第 5、9 天的**DOM 与渲染模式** | 交互的基础 |
| 5 | 各天的**踩坑记录** | 快速过一遍 |
