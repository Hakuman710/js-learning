# JS 速查表（Day 1-10）

学到的东西按知识点速查。详细说明见 `D:\职业规划\JS学习总纲-Day1-10.md`。

---

## 变量与类型

```javascript
let 名字 = "张三";          // 可改
const 圆周率 = 3.14;        // 不可改

typeof "abc"                // "string"
typeof 123                  // "number"
typeof []                   // "object"

Number("20")                // 20
String(20)                  // "20"
```

**6 个假值**：`false`、`0`、`""`、`null`、`undefined`、`NaN`
**其他全是真值**（包括 `[]` 和 `{}`）

**`||` 兜底**：`读到的值 || 默认值`

---

## 运算符

```javascript
+  -  *  /  %              // % 取余
>  <  >=  <=
===   !==                  // 判断相等用 ===
&&    ||    !              // 与、或、非
i++   i--                  // 自增、自减
条件 ? 值1 : 值2            // 三元
```

---

## 条件

```javascript
if (条件) { }
else if (条件) { }
else { }

if (内容 === "") return;        // 提前结束函数
```

---

## 循环

```javascript
for (let i = 0; i < 数组.length; i++) { }

// 累积模式
let 总 = 0;
for (...) { 总 += 数组[i]; }
console.log(总);              // 输出在循环外
```

**赋值 / 输出写在循环外面。**

---

## 字符串

```javascript
"abc".length                // 3
"abc".split(",")            // 拆成数组
"a,b".toUpperCase()         // 大写

// 模板字符串（反引号）
`你好，${名字}！`
`<div class="${类名}">${内容}</div>`      // 可多行
```

---

## 数组

```javascript
let a = [85, 92, 78];

a[0]                        // 85（下标从 0 开始）
a.length                    // 3
a.push(88)                  // 末尾加
a.splice(1, 1)              // 从下标1删1个（改原数组）
a.join(",")                 // "85,92,78"
```

### 数组方法

| 方法 | 返回 | 用途 |
|---|---|---|
| `map` | 新数组 | 每个元素转换 |
| `filter` | 新数组 | 筛选（**要赋值回去**）|
| `forEach` | `undefined` | 只遍历 |

```javascript
a.map(function (项, i) { return `<div>${项}</div>`; }).join("")

a.filter(function (项) { return 项 > 80; })

a.forEach(function (项) { console.log(项); })
```

`filter` 用法：`列表 = 列表.filter(...)`

---

## 对象

```javascript
let 人 = { 名字: "张三", 年龄: 20 };

人.名字                    // "张三"
人.年龄 = 21;              // 改

// 数组装对象（最常用结构）
let 列表 = [
    { 文字: "写作业", 完成: false },
    { 文字: "学 JS",  完成: true  }
];
列表[0].文字               // "写作业"
列表.push({ 文字: "买菜", 完成: false });
```

---

## 函数

```javascript
function 加法(a, b) {      // 定义
    return a + b;
}
加法(3, 4);                // 调用

// 三种写法
function f() { }
function () { }
() => { }

// 回调：传函数不加括号
按钮.addEventListener("click", 处理);
```

---

## 作用域

| 写在哪 | 行为 |
|---|---|
| 函数/事件**外面** | 一直活着，**记住上次的值** |
| 函数/事件**里面** | **每次都是新的** |

判断：**这个变量需要记住上一次吗？** 需要 → 外面。

---

## DOM

```javascript
document.getElementById("id名")     // 找元素（script 要在元素后面）
元素.innerText = "纯文字"
元素.innerHTML = "<b>生效的标签</b>"
元素.value                          // 读输入框（是字符串！）
元素.value = ""                     // 清空
元素.className = "已完成"
元素.style.color = "red"
```

| 输出方式 | 到哪 |
|---|---|
| `console.log()` | F12 控制台 |
| `alert()` | 弹窗 |
| `元素.innerHTML = ` | 页面 |

---

## 事件

```javascript
元素.addEventListener("click", function () { });
```

| 事件 | 触发 |
|---|---|
| `"click"` | 点击 |
| `"input"` | 输入变化 |
| `"change"` | 输入完成 / 勾选框变化 |

**动态生成的元素**用属性绑定：

```javascript
`<button onclick="删除(${i})">删除</button>`

function 删除(下标) { }        // 必须是全局函数
```

---

## localStorage

```javascript
localStorage.setItem("键", "值")
localStorage.getItem("键")            // 不存在时返回 null
localStorage.removeItem("键")

JSON.stringify(数据)                  // 打包：数据 → 字符串
JSON.parse(字符串)                    // 拆包：字符串 → 数据
```

**完整模式**：

```javascript
// 读
let 列表 = JSON.parse(localStorage.getItem("待办")) || [];

// 存
function 保存() {
    localStorage.setItem("待办", JSON.stringify(列表));
}

// 渲染
function 渲染() {
    let 内容 = "";
    for (...) { 内容 += ...; }
    容器.innerHTML = 内容;
}

// 改 → 存 → 渲染
列表.push(新项); 保存(); 渲染();

// 页面打开时画一次（别忘）
渲染();
```

**查看数据**：F12 → Application → Local Storage

---

## CSS

```css
#id { }            /* 唯一元素 */
标签 { }           /* 所有该标签 */
.class { }         /* 可重复 */
.class:hover { }   /* 鼠标悬停 */
```

| 属性 | 作用 |
|---|---|
| `color` / `background` | 文字色 / 背景色 |
| `font-size` | 字号 |
| `padding` / `margin` | 内边距 / 外边距 |
| `border` / `border-radius` | 边框 / 圆角 |
| `text-align` | 文字对齐 |
| `width` / `max-width` | 宽度 / 最大宽度 |
| `text-decoration: line-through` | 删除线 |

```css
* { box-sizing: border-box; }     /* 让 width 包含 padding */

.容器 {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 10px;
}

.居中 { max-width: 900px; margin: 0 auto; text-align: center; }
```

**display**：`block` 独占一行 / `inline` 挤一行不能设宽高 / **`inline-block` 能排一行又能设宽高**

**布局计算**：一行几个 = 容器宽 ÷ 元素总宽（向下取整）

---

## HTML 标签

```html
<div>        容器（独占一行）
<span>       行内容器
<h1>         标题
<input>      输入框（type="text" / "number" / "checkbox"）
<button>     按钮
<label>      点文字 = 点控件
<br>         换行
<hr>         分割线
<!-- 注释 -->      ← // 在 HTML 里不是注释！
```

---

## 调试

| 面板 | 用途 |
|---|---|
| Console | 看输出和报错 |
| Elements | 看 HTML 结构和生效的 CSS |
| Application | 看 / 改 Local Storage |

```javascript
console.log("到了这里", 变量);
console.log(typeof 变量);
```

---

## 踩坑清单

| 坑 | 正解 |
|---|---|
| 判断相等写成 `=` | 用 `===` |
| `.value` / `prompt` 当数字用 | 先 `Number()` |
| `let` 写在循环里 | 写循环外面 |
| `innerHTML =` 写在循环里 | 写循环外面 |
| 变量写在事件外导致累加 | 每次重置的写里面 |
| `[] \|\| 数据` 顺序反 | `数据 \|\| []` |
| `filter` 忘赋值回去 | `列表 = 列表.filter(...)` |
| 数组直接存 localStorage | 先 `JSON.stringify` |
| HTML 里用 `//` 注释 | 用 `<!-- -->` |
| 传回调函数加了括号 | 不加 `()` |
| 忘了页面打开时 `渲染()` | 脚本最后加一行 |
